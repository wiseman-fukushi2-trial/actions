name: Build
description: ビルド

inputs:
  project-files:
    description: 処理対象プロジェクトファイルのリスト
    required: true

runs:
  using: composite

  steps:
    - name: Build
      shell: cmd
      run: |
          echo ${{ inputs.project-files }}
