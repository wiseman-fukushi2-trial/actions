name: File Check Unit Test
description: ファイルチェックの UnitTest

runs:
  using: composite

  steps:
    - name: Unit Test
      shell: pwsh
      run: |
        dotnet-script "${{ github.action_path }}/UnitTest.csx"
